import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // react-konva needs client-side only rendering
  // No special config needed — we handle it via dynamic imports
};

export default nextConfig;
